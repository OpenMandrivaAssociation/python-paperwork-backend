--- a/pyproject.toml
+++ b/pyproject.toml
@@ -29,9 +29,9 @@ lint = [
 ]
 
 [tool.setuptools_scm]
-root = ".."
+root = "."
 relative_to = "__file__"
-write_to = "paperwork-backend/src/paperwork_backend/_version.py"
+write_to = "src/paperwork_backend/_version.py"
 
 [build-system]
 requires = ["setuptools>=45", "setuptools_scm[toml]>=6.2"]
